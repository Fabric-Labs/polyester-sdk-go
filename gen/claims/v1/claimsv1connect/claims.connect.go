// Code generated by protoc-gen-connect-go. DO NOT EDIT.
//
// Source: claims/v1/claims.proto

package claimsv1connect

import (
	connect "connectrpc.com/connect"
	context "context"
	errors "errors"
	v1 "github.com/Fabric-Labs/polyester-sdk-go/gen/claims/v1"
	http "net/http"
	strings "strings"
)

// This is a compile-time assertion to ensure that this generated file and the connect package are
// compatible. If you get a compiler error that this constant is not defined, this code was
// generated with a version of connect newer than the one compiled into your binary. You can fix the
// problem by either regenerating this code with an older version of connect or updating the connect
// version compiled into your binary.
const _ = connect.IsAtLeastVersion1_13_0

const (
	// ClaimsServiceName is the fully-qualified name of the ClaimsService service.
	ClaimsServiceName = "claims.v1.ClaimsService"
)

// These constants are the fully-qualified names of the RPCs defined in this package. They're
// exposed at runtime as Spec.Procedure and as the final two segments of the HTTP route.
//
// Note that these are different from the fully-qualified method names used by
// google.golang.org/protobuf/reflect/protoreflect. To convert from these constants to
// reflection-formatted method names, remove the leading slash and convert the remaining slash to a
// period.
const (
	// ClaimsServiceGetDailyClaimStatusProcedure is the fully-qualified name of the ClaimsService's
	// GetDailyClaimStatus RPC.
	ClaimsServiceGetDailyClaimStatusProcedure = "/claims.v1.ClaimsService/GetDailyClaimStatus"
	// ClaimsServiceClaimDailyRewardProcedure is the fully-qualified name of the ClaimsService's
	// ClaimDailyReward RPC.
	ClaimsServiceClaimDailyRewardProcedure = "/claims.v1.ClaimsService/ClaimDailyReward"
)

// ClaimsServiceClient is a client for the claims.v1.ClaimsService service.
type ClaimsServiceClient interface {
	// Get today's reward and claim status for the authenticated root account. Future reward schedules are not returned.
	GetDailyClaimStatus(context.Context, *connect.Request[v1.GetDailyClaimStatusRequest]) (*connect.Response[v1.GetDailyClaimStatusResponse], error)
	// Claim today's reward once for the authenticated root account. All reward assets are transferred together, and retries return the same claim.
	ClaimDailyReward(context.Context, *connect.Request[v1.ClaimDailyRewardRequest]) (*connect.Response[v1.ClaimDailyRewardResponse], error)
}

// NewClaimsServiceClient constructs a client for the claims.v1.ClaimsService service. By default,
// it uses the Connect protocol with the binary Protobuf Codec, asks for gzipped responses, and
// sends uncompressed requests. To use the gRPC or gRPC-Web protocols, supply the connect.WithGRPC()
// or connect.WithGRPCWeb() options.
//
// The URL supplied here should be the base URL for the Connect or gRPC server (for example,
// http://api.acme.com or https://acme.com/grpc).
func NewClaimsServiceClient(httpClient connect.HTTPClient, baseURL string, opts ...connect.ClientOption) ClaimsServiceClient {
	baseURL = strings.TrimRight(baseURL, "/")
	claimsServiceMethods := v1.File_claims_v1_claims_proto.Services().ByName("ClaimsService").Methods()
	return &claimsServiceClient{
		getDailyClaimStatus: connect.NewClient[v1.GetDailyClaimStatusRequest, v1.GetDailyClaimStatusResponse](
			httpClient,
			baseURL+ClaimsServiceGetDailyClaimStatusProcedure,
			connect.WithSchema(claimsServiceMethods.ByName("GetDailyClaimStatus")),
			connect.WithClientOptions(opts...),
		),
		claimDailyReward: connect.NewClient[v1.ClaimDailyRewardRequest, v1.ClaimDailyRewardResponse](
			httpClient,
			baseURL+ClaimsServiceClaimDailyRewardProcedure,
			connect.WithSchema(claimsServiceMethods.ByName("ClaimDailyReward")),
			connect.WithClientOptions(opts...),
		),
	}
}

// claimsServiceClient implements ClaimsServiceClient.
type claimsServiceClient struct {
	getDailyClaimStatus *connect.Client[v1.GetDailyClaimStatusRequest, v1.GetDailyClaimStatusResponse]
	claimDailyReward    *connect.Client[v1.ClaimDailyRewardRequest, v1.ClaimDailyRewardResponse]
}

// GetDailyClaimStatus calls claims.v1.ClaimsService.GetDailyClaimStatus.
func (c *claimsServiceClient) GetDailyClaimStatus(ctx context.Context, req *connect.Request[v1.GetDailyClaimStatusRequest]) (*connect.Response[v1.GetDailyClaimStatusResponse], error) {
	return c.getDailyClaimStatus.CallUnary(ctx, req)
}

// ClaimDailyReward calls claims.v1.ClaimsService.ClaimDailyReward.
func (c *claimsServiceClient) ClaimDailyReward(ctx context.Context, req *connect.Request[v1.ClaimDailyRewardRequest]) (*connect.Response[v1.ClaimDailyRewardResponse], error) {
	return c.claimDailyReward.CallUnary(ctx, req)
}

// ClaimsServiceHandler is an implementation of the claims.v1.ClaimsService service.
type ClaimsServiceHandler interface {
	// Get today's reward and claim status for the authenticated root account. Future reward schedules are not returned.
	GetDailyClaimStatus(context.Context, *connect.Request[v1.GetDailyClaimStatusRequest]) (*connect.Response[v1.GetDailyClaimStatusResponse], error)
	// Claim today's reward once for the authenticated root account. All reward assets are transferred together, and retries return the same claim.
	ClaimDailyReward(context.Context, *connect.Request[v1.ClaimDailyRewardRequest]) (*connect.Response[v1.ClaimDailyRewardResponse], error)
}

// NewClaimsServiceHandler builds an HTTP handler from the service implementation. It returns the
// path on which to mount the handler and the handler itself.
//
// By default, handlers support the Connect, gRPC, and gRPC-Web protocols with the binary Protobuf
// and JSON codecs. They also support gzip compression.
func NewClaimsServiceHandler(svc ClaimsServiceHandler, opts ...connect.HandlerOption) (string, http.Handler) {
	claimsServiceMethods := v1.File_claims_v1_claims_proto.Services().ByName("ClaimsService").Methods()
	claimsServiceGetDailyClaimStatusHandler := connect.NewUnaryHandler(
		ClaimsServiceGetDailyClaimStatusProcedure,
		svc.GetDailyClaimStatus,
		connect.WithSchema(claimsServiceMethods.ByName("GetDailyClaimStatus")),
		connect.WithHandlerOptions(opts...),
	)
	claimsServiceClaimDailyRewardHandler := connect.NewUnaryHandler(
		ClaimsServiceClaimDailyRewardProcedure,
		svc.ClaimDailyReward,
		connect.WithSchema(claimsServiceMethods.ByName("ClaimDailyReward")),
		connect.WithHandlerOptions(opts...),
	)
	return "/claims.v1.ClaimsService/", http.HandlerFunc(func(w http.ResponseWriter, r *http.Request) {
		switch r.URL.Path {
		case ClaimsServiceGetDailyClaimStatusProcedure:
			claimsServiceGetDailyClaimStatusHandler.ServeHTTP(w, r)
		case ClaimsServiceClaimDailyRewardProcedure:
			claimsServiceClaimDailyRewardHandler.ServeHTTP(w, r)
		default:
			http.NotFound(w, r)
		}
	})
}

// UnimplementedClaimsServiceHandler returns CodeUnimplemented from all methods.
type UnimplementedClaimsServiceHandler struct{}

func (UnimplementedClaimsServiceHandler) GetDailyClaimStatus(context.Context, *connect.Request[v1.GetDailyClaimStatusRequest]) (*connect.Response[v1.GetDailyClaimStatusResponse], error) {
	return nil, connect.NewError(connect.CodeUnimplemented, errors.New("claims.v1.ClaimsService.GetDailyClaimStatus is not implemented"))
}

func (UnimplementedClaimsServiceHandler) ClaimDailyReward(context.Context, *connect.Request[v1.ClaimDailyRewardRequest]) (*connect.Response[v1.ClaimDailyRewardResponse], error) {
	return nil, connect.NewError(connect.CodeUnimplemented, errors.New("claims.v1.ClaimsService.ClaimDailyReward is not implemented"))
}
